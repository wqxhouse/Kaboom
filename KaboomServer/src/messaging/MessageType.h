#pragma once

enum class MessageType {
    COLLISION,
    EXPLOSION,
    TIMER_EXPIRED,
    ATTACK1,
    ATTACK2,
    NO_ATTACK,
    PICKUP,
    CHARACTER_SPAWN,
	JUMP_PAD,
	BOMB_DROP,
	BOMB_DROP_DESPAWN,
};
